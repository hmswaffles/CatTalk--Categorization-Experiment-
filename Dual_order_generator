import random

def Dual_order_gen(n):
    numb = []
    for i in range(0,359):

        #begin 10% section
        if random.randint(0,9) == 4:
            nextnumb = numb[i-(n-1)]
        else:
            nextnumb = random.randin(1,15)
        numb.append(nextnumb)
    return numb
            
